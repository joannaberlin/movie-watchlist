export const utils = {};

utils.getElem = (string, imdbID) => {
	return document.getElementById(string + imdbID);
};

// const getDupa = () => {
// 	console.log('dupa');
// };

// export default getElem;
